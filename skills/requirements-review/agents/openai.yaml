interface:
  display_name: "Requirements Review"
  short_description: "Review one approved specification target"
  default_prompt: "Use $requirements-review to compare the implementation with the selected global or feature specification."
