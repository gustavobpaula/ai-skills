interface:
  display_name: "Architecture Review"
  short_description: "Review architecture for one target"
  default_prompt: "Use $arch-review to compare the selected feature implementation with docs/ARCHITECTURE.md and its approved specification drivers."
