interface:
  display_name: "Architecture Review"
  short_description: "Detect architectural drift and excess"
  default_prompt: "Use $arch-review to compare the current changes with docs/ARCHITECTURE.md and report architectural drift or overengineering."
